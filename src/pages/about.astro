---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About">
  <section class="grid md:grid-cols-2 gap-8 items-center" data-animate>
    <div>
      <h1 class="text-3xl md:text-4xl font-extrabold text-white">About James</h1>
      <p class="mt-3 text-zinc-400">
        I build fast, content-driven websites using Astro + Tailwind. I care about accessibility,
        performance, and maintainable UI architecture.
      </p>
      <div class="mt-6 flex gap-3">
        <span class="badge">Performance</span>
        <span class="badge">Content-first</span>
        <span class="badge">A11y</span>
      </div>
    </div>
    <div class="rounded-2xl overflow-hidden border border-white/10">
      <img src="/images/james.jpg" alt="" class="h-full w-full object-cover" loading="lazy" />
    </div>
  </section>

  <!-- Skills -->
  <section class="mt-12 md:mt-16" data-animate>
    <h2 class="text-2xl md:text-3xl font-bold text-white">Skills</h2>
    <div class="mt-6 grid grid-cols-2 md:grid-cols-4 gap-3">
      {['Astro','Tailwind','Markdown','Git & PRs','Accessibility','Responsive','Vercel','Components'].map(s => (
        <span class="text-center card card-surface text-zinc-200">{s}</span>
      ))}
    </div>
  </section>

  <!-- Timeline -->
  <section class="mt-12 md:mt-16" data-animate>
    <h2 class="text-2xl md:text-3xl font-bold text-white">Timeline</h2>
    <ol class="relative border-s border-white/10 mt-6 space-y-8">
      <li class="ms-6">
        <span class="absolute -start-2.5 mt-1 size-5 rounded-full border border-white/20 bg-zinc-900/80"></span>
        <div class="card card-surface">
          <div class="flex items-center justify-between gap-4">
            <h3 class="text-lg font-semibold text-white">Modern Astro Sites</h3>
            <span class="text-xs text-zinc-500">2025 — now</span>
          </div>
          <p class="mt-2 text-zinc-400">Content-first builds with zero-JS by default and strong contrast.</p>
        </div>
      </li>
      <li class="ms-6">
        <span class="absolute -start-2.5 mt-1 size-5 rounded-full border border-white/20 bg-zinc-900/80"></span>
        <div class="card card-surface">
          <div class="flex items-center justify-between gap-4">
            <h3 class="text-lg font-semibold text-white">Reusable UI Systems</h3>
            <span class="text-xs text-zinc-500">Ongoing</span>
          </div>
          <p class="mt-2 text-zinc-400">Buttons, cards, sections — utility-first, accessible patterns.</p>
        </div>
      </li>
    </ol>
  </section>
</Layout>
