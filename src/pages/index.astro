---
import Layout from '../layouts/Layout.astro';
import LatestPosts from '../components/LatestPosts.astro'; // <- keep if you created it
---

<Layout title="Home">
  <!-- HERO -->
  <section class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-indigo-700 via-indigo-600 to-fuchsia-700 text-white px-6 py-16 md:px-12 md:py-20">
    <!-- subtle floating accents -->
    <div class="absolute right-6 top-6 h-24 w-24 rounded-full bg-white/15 blur-xl animate-float"></div>
    <div class="absolute -left-8 -bottom-8 h-40 w-40 rounded-full bg-white/10 blur-2xl animate-float"></div>

    <div class="max-w-3xl" data-animate>
      <p class="uppercase tracking-widest text-white/80 text-xs mb-3">Astro • Tailwind • Git</p>

      <h1 class="text-4xl md:text-6xl font-extrabold leading-tight text-white">
        Build <span class="relative">
          ultra-fast
          <span class="absolute inset-x-0 bottom-1 h-[10px] bg-white/30 -z-10"></span>
        </span> websites the right way.
      </h1>

      <p class="mt-4 md:text-lg text-zinc-100/90">
        I’m James. I ship modern, content-driven sites with clean code, smart performance, and a professional workflow.
      </p>

      <div class="mt-8 flex flex-col sm:flex-row gap-3">
        <a href="/projects" class="btn-primary">View Projects</a>
        <a href="/blog" class="btn-secondary">Read the Blog</a>
      </div>

      <!-- quick tech row -->
      <div class="mt-8 grid grid-cols-2 sm:grid-cols-4 gap-6 opacity-90" data-animate>
        <div class="text-center text-sm text-zinc-100">⚡ Astro</div>
        <div class="text-center text-sm text-zinc-100">🎨 Tailwind</div>
        <div class="text-center text-sm text-zinc-100">🧩 Components</div>
        <div class="text-center text-sm text-zinc-100">🚀 Vercel</div>
      </div>
    </div>
  </section>

  <!-- FEATURE GRID -->
  <section class="mt-12 md:mt-16">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="card card-surface" data-animate>
        <div class="text-2xl">🚀</div>
        <h3 class="text-xl font-semibold mt-2 text-white">Performance First</h3>
        <p class="text-zinc-400 mt-1">Zero-JS by default, image-aware layouts, and best practices baked in.</p>
      </div>
      <div class="card card-surface" data-animate>
        <div class="text-2xl">🧱</div>
        <h3 class="text-xl font-semibold mt-2 text-white">Clean Components</h3>
        <p class="text-zinc-400 mt-1">Composable sections, reusable UI, and maintainable patterns.</p>
      </div>
      <div class="card card-surface" data-animate>
        <div class="text-2xl">🔁</div>
        <h3 class="text-xl font-semibold mt-2 text-white">Pro Workflow</h3>
        <p class="text-zinc-400 mt-1">Git branches, PRs, previews, and automated deploys.</p>
      </div>
    </div>
  </section>

  <!-- STATS STRIP -->
  <section class="mt-12 md:mt-16 rounded-2xl border border-white/10 bg-zinc-900/60 p-6 md:p-8" data-animate>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 text-center">
      <div>
        <div class="text-3xl font-extrabold text-white">95+</div>
        <div class="text-zinc-500 text-sm">Lighthouse Performance</div>
      </div>
      <div>
        <div class="text-3xl font-extrabold text-white">0kb</div>
        <div class="text-zinc-500 text-sm">JS sent by default</div>
      </div>
      <div>
        <div class="text-3xl font-extrabold text-white">∞</div>
        <div class="text-zinc-500 text-sm">Scales with content</div>
      </div>
    </div>
  </section>

  <!-- LATEST POSTS (keep only if component exists) -->
  <section class="mt-12 md:mt-16" data-animate>
    <LatestPosts />
  </section>

  <!-- FINAL CTA -->
  <section class="mt-12 md:mt-16 rounded-2xl border border-white/10 bg-zinc-900/60 p-8 md:p-12 flex flex-col md:flex-row items-start md:items-center justify-between gap-6" data-animate>
    <div>
      <h2 class="text-2xl md:text-3xl font-bold text-white">Let’s ship something exceptional.</h2>
      <p class="text-zinc-400 mt-1">From concept to production — fast, accessible, and beautiful.</p>
    </div>
    <a href="/contact" class="btn-primary">Get in Touch →</a>
  </section>
</Layout>
